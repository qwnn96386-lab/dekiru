name: my_first_app
description: "A new Flutter project for permission testing."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  permission_handler: ^11.3.1
  # 以下為新增實作邏輯所需套件
  sqflite: ^2.3.0
  path: ^1.8.3
  geolocator: ^10.1.0
  geofence_service: ^6.0.0+1
  flutter_local_notifications: ^17.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true